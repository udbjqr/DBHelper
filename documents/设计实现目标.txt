数据库配置使用json方式。指定的名称在代码当中直接使用

数据库的连接池
可以连接多个数据库
根据参数类型自动处理参数的引号等
简化操作数据库代码（使用select方法时直接使用回调）
请求连接时每个线程给一个独立的连接
	好处是可以使用一个begin tran方法后，多处调用完再commit.
	在一个请求当中调用请求时直接返回同一个连接对象
数据查询可以使用限定行数

给一个查询语句后，直接使用对象化的数据
主从表的组合查询
将条件、限制等等分成对象处理。
保存本次更新了什么值。旧值为多少。做记录

